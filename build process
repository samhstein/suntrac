format sd card
flash using berrylan image
login to pi: pi / raspberry
change password: 123st321
use berrylan to connect to local wifi, note ip address
ssh to pi
sudo apt-get update
sudo apt-get upgrade
raspi-config
  turn i2c on
  turn off serial remote
  turn on serial port
  it will reboot on finish

ssh to pi
sudo apt-get install build-essential python3-pip python3-dev python3-smbus git-core memcached
cd
git clone git://git.drogon.net/wiringPi
cd wiringPi
./build
  gpio -v
  gpio readall

cd
git clone https://github.com/samhstein/suntrac.git
cd suntrac
pip3 install -r requirements.txt

cd sequent
unzip megaiosun.zip
cd megaiosun-rpi/python/megaiosun
sudo python3 setup.py install
cd
cd suntrac/test
python3 suntrac_test.py

cd
cd suntrac/panel
sudo cp suntracd.service /etc/systemd/system/suntracd.service
sudo systemctl enable suntracd.service

### to flash board
sudo systemctl stop suntracd.service
cd /home/pi/megaiosun-rpi
sequent/megaiosun-rpi/update
./update

suntrac
ssh pi@184.186.223.251

### to clone disk
clone
  cd ???
  sudo dd if=/dev/rdiskN of=suntrac_clone.dmg bs=4194304
  diskutil unmountDisk diskN
  sudo dd of=/dev/rdiskN if=suntrac_clone.dmg bs=4194304
